<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mikumifa.wms.dao.mapper.RailwayInfoMapper">
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        DELETE FROM railway_info WHERE route_id = #{routeId}
    </delete>

    <insert id="insert" parameterType="com.mikumifa.wms.entity.dto.RailwayInfo">
        INSERT INTO railway_info (origin, destination, departure_time,arrival_time)
        VALUES (#{origin}, #{destination}, #{departureTime},#{arrivalTime})
    </insert>

    <insert id="insertSelective" parameterType="com.mikumifa.wms.entity.dto.RailwayInfo">
        INSERT INTO railway_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="origin != null">origin,</if>
            <if test="destination != null">destination,</if>
            <if test="departureTime != null">departure_time,</if>
            <if test="arrivalTime != null">arrival_time,</if>

        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="origin != null">#{origin},</if>
            <if test="destination != null">#{destination},</if>
            <if test="departure_time != null">#{departureTime},</if>
            <if test="arrival_time != null">#{arrivalTime},</if>
        </trim>
    </insert>

    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultType="com.mikumifa.wms.entity.dto.RailwayInfo">
        SELECT route_id,origin, destination, departure_time,arrival_time
        FROM railway_info WHERE route_id = #{routeId}
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.mikumifa.wms.entity.dto.RailwayInfo">
        UPDATE railway_info
        <set>
            <if test="origin != null">#{origin},</if>
            <if test="destination != null">#{destination},</if>
            <if test="departure_time != null">#{departureTime},</if>
            <if test="arrival_time != null">#{arrivalTime},</if>
        </set>
        WHERE route_id = #{routeId}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.mikumifa.wms.entity.dto.RailwayInfo">
        UPDATE railway_info SET
                                origin = #{origin},
                                destination = #{destination},
                                departure_time = #{departureTime},
                                arrival_time = #{arrivalTime}
        WHERE route_id = #{routeId}
    </update>
</mapper>